# ============================================================
# Pudcraft Community — 项目规范、约束与注意事项
# ============================================================
# 本文件是 AI 协作编码的核心约束文件。
# 所有涉及代码修改、架构决策、新增功能的操作都必须遵守以下规则。
# ============================================================

## 1. 文件 / 目录组织规则

### 目录职责（严格分离）

| 目录                       | 职责                                 | 禁止放入                         |
| -------------------------- | ------------------------------------ | -------------------------------- |
| `src/app/`                 | Next.js App Router 页面 & API 路由   | 业务逻辑、工具函数               |
| `src/app/api/`             | REST API Route Handlers              | 页面组件、抓取逻辑               |
| `src/app/admin/`           | 管理员后台页面                       | 普通用户功能                     |
| `src/app/console/`         | 服主控制台页面                       | 管理员功能、普通浏览功能         |
| `src/components/`          | 可复用 UI 组件                       | API 调用、数据库访问             |
| `src/components/console/`  | 控制台专用组件（图表、统计卡片等）   | 通用 UI 组件                     |
| `src/hooks/`               | 自定义 React Hooks                   | 组件、API 逻辑                   |
| `src/lib/`                 | 工具函数、第三方服务封装、DB 客户端  | React 组件、路由定义             |
| `src/worker/`              | 后台 Worker 进程入口                 | API Route、页面组件              |
| `src/types/`               | TypeScript 类型声明文件              | 业务逻辑                         |
| `src/styles/`              | 全局样式                             | 组件级样式（用 Tailwind 类名）   |
| `prisma/`                  | Prisma Schema 和数据库迁移           | 应用代码                         |
| `public/uploads/`          | 本地文件存储（头像、图标）           | 代码文件（已 gitignore）         |

### 命名规范

- **组件文件**：PascalCase（如 `ServerCard.tsx`）
- **工具/库文件**：camelCase（如 `validation.ts`、`storage.ts`）
- **页面文件**：`page.tsx`（Next.js 约定）
- **API Route**：`route.ts`（Next.js 约定）
- **组件名**：PascalCase（`export function ServerCard`）
- **函数/变量**：camelCase（`export function uploadServerIcon`）
- **常量**：UPPER_SNAKE_CASE（`const QUEUE_NAME = "server-ping"`)
- **类型/接口**：PascalCase，不加 `I` 前缀（`interface ServerCardProps`）
- **Zod Schema**：camelCase + `Schema` 后缀（`createServerSchema`）
- **数据库表名**：snake_case 复数，通过 `@@map` 映射（`@@map("servers")`）
- **数据库字段映射**：snake_case，通过 `@map` 映射

### 新增文件规则

- 新的 UI 组件 → `src/components/`
- 控制台专用组件 → `src/components/console/`
- 新的页面 → `src/app/` 下对应路由
- 新的 API → `src/app/api/` 下对应路径
- 管理员 API → `src/app/api/admin/`
- 新的工具函数 → `src/lib/`
- 新的 React Hook → `src/hooks/`
- 新的 Zod Schema → `src/lib/validation.ts`（集中管理）
- 新的类型声明 → `src/types/`

---

## 2. 提交规则

- 每次提交**聚焦于单一变更**：一个功能 / 一个修复 / 一次重构
- **不要**在功能提交中混入重构代码
- **不要**在重构提交中混入功能变更
- Commit message 推荐格式：`<type>: <description>`
  - `feat:` 新功能
  - `fix:` 修复 Bug
  - `refactor:` 重构（无功能变化）
  - `style:` 代码格式调整（非 UI 样式）
  - `docs:` 文档更新
  - `chore:` 构建/配置/依赖变更
  - `perf:` 性能优化
- 提交前运行 `pnpm lint` 和 `pnpm tsc --noEmit` 确保无报错
- 提交前确认 `.env` 未被 stage

---

## 3. 安全规则

### 密钥管理

- **绝不**把任何密钥、Token、密码、连接字符串写入代码或仓库
- 所有敏感配置通过 `.env.local` 文件管理
- 仓库中只保留 `.env.example`（只有键名，无真实值）
- `.env` / `.env.local` / `.env.production` 已加入 `.gitignore`

### API 输入校验

- 所有 API 端点的输入 **必须** 使用 Zod Schema 校验
- **不要信任** 任何来自客户端/外部的数据
- `address` 字段：校验格式，禁止 `localhost` / `127.0.0.1` / 内网 IP
- `port` 字段：限制 1-65535 范围
- 字符串字段：限制长度上限
- 数组字段：限制元素数量上限
- 邮箱字段：统一 `.toLowerCase().trim()` 处理

### 权限校验

- 所有写操作 **必须在服务端验证权限**，不能只靠前端隐藏按钮
- 用户角色：`user`（默认）| `admin`
- 权限层级：
  - **公开**：服务器列表、详情页、用户公开主页
  - **登录用户**：评论、收藏、提交服务器、认领
  - **Owner**：编辑/删除自己的服务器、查看服务器统计数据
  - **Admin**：审核服务器、封禁用户、管理后台
- 认领机制：任何登录用户可通过 MOTD Token 验证认领服务器，认领成功后成为 owner
- 被封禁用户（`isBanned: true`）无法登录

### 防滥用

- 邮箱验证码：同一邮箱 60 秒冷却，同一 IP 每天 10 封上限
- 验证码错误 5 次后锁定 15 分钟
- 评论发表：每用户每分钟最多 5 条
- 收藏操作：每用户每分钟最多 30 次
- 注册 API：不区分"用户不存在"和"密码错误"（防枚举）
- 找回密码 API：不论邮箱是否存在都返回相同响应（防枚举）

### 外链处理

- 用户提交的外部链接必须添加 `rel="noopener noreferrer"` 和 `target="_blank"`
- Markdown 渲染使用 `react-markdown`（自带 sanitize）
- 评论内容使用文本节点渲染，**禁止** `dangerouslySetInnerHTML`

---

## 4. 性能规则

### 核心原则

- **禁止**在页面请求（Server Component / API Route）中直接 ping Minecraft 服务器
- 服务器状态 **必须** 通过 Worker 异步获取，结果写入数据库
- 前端从 DB 的 Server 表缓存字段读取状态数据（`isOnline`、`playerCount` 等）
- 页面渲染不得依赖任何外部网络请求的实时结果
- 首页使用 SSR 直出首屏数据，交互操作走客户端请求

### 超时策略

| 场景                    | 超时时间  |
| ----------------------- | --------- |
| MC 服务器 ping          | 5 秒      |
| Worker 单任务处理       | 10 秒     |
| API Route 响应目标      | < 200ms   |
| 数据库查询告警阈值      | > 100ms   |
| MOTD 验证等待           | 15 秒     |

### 重试策略

- ping Worker 任务失败不重试（`attempts: 1`），下一轮自动重新 ping
- 验证 Worker 任务失败不重试，用户可手动重试
- 通知创建失败不阻塞主操作，只记日志

### 缓存策略

- Server 表直接缓存 `isOnline`、`playerCount`、`maxPlayers`、`latency`、`favoriteCount`，避免 join 查询
- 收藏 ID 列表通过 `/api/user/favorites/ids` 批量查询，不逐个请求
- 未读通知数量每 30 秒轮询一次

---

## 5. UI 规则

### 设计原则

- **Material 3 浅色主题**
- 品牌色：`#e2f4f7`（浅青色）
- 强调色：teal 系（`teal-600` / `teal-500`）
- 背景：白色 / 极浅灰
- 卡片：白底 + 浅灰边框（`border-gray-200`）+ 圆角（`rounded-xl`）+ 轻微阴影
- 按钮：轮廓式（outline）为主，重要操作用 teal 填充
- 标签 chips：浅灰底 `bg-gray-100 text-gray-600`，选中态深色填充
- **克制原则**：不使用渐变背景、不使用 3D 阴影、不使用复杂动画

### 响应式设计

- **移动端优先**：默认样式为手机，用 `md:` `lg:` 添加桌面样式
- 断点：`sm:640px` `md:768px` `lg:1024px`
- 卡片网格：`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
- 导航栏：桌面水平导航，移动端汉堡菜单
- 标签筛选：窄屏横向滚动（`overflow-x-auto`）

### ServerCard 卡片字段顺序（固定）

1. 服务器名称 + 在线状态指示灯 + 认领标识（如有）+ 收藏按钮
2. 服务器地址（`host:port`，非默认端口才显示端口）
3. 简短描述（最多 2 行截断 `line-clamp-2`）
4. 在线人数 / 最大人数 + 延迟毫秒
5. 标签 Chips

### 颜色语义（固定）

| 语义             | 颜色            |
| ---------------- | --------------- |
| 在线             | `emerald-500`   |
| 离线             | `gray-400`      |
| 低延迟 < 50ms   | `teal-600`      |
| 中延迟 50-100ms  | `yellow-500`    |
| 高延迟 > 100ms   | `red-500`       |
| 认领标识         | `teal-600`      |
| 错误提示         | `red-500`       |
| 成功提示         | `green-600`     |

### 图片处理

- 所有用户上传图片前端裁切为 1:1 + 压缩为 WebP
- 文件选择器使用 `accept="image/*"`，JS 端精确校验类型
- 头像输出尺寸 256px，服务器图标 512px
- 上传区域文案不包含技术性词汇（WebP、裁切、压缩）
- 无头像/图标时显示首字母占位符或默认图片

### 组件规范

- 所有 UI 组件放在 `src/components/`
- 使用 **命名导出**（`export function`），不使用默认导出
- 组件文件名与组件名一致（PascalCase）
- Props 使用独立的 `interface` 定义
- 使用 Next.js `<Image>` 组件替代 `<img>` 标签
- 使用统一的 Toast 系统显示操作反馈
- 使用统一的 EmptyState 组件显示空状态
- 使用统一的 LoadingSpinner 组件显示加载状态
- 收藏按钮使用 `<button>` + `e.stopPropagation()`，不用 `<a>`

---

## 6. 数据库规则

### Prisma 模型变更流程

1. 修改 `prisma/schema.prisma`
2. 开发环境：`pnpm prisma migrate dev --name describe_your_change`
3. 迁移命名使用描述性 snake_case：`add_server_verification`、`add_notifications`
4. 生产环境：`prisma migrate deploy`
5. **禁止**在生产环境使用 `db push`

### 时间字段统一

- 所有时间字段使用 `DateTime` 类型
- 创建时间：`createdAt DateTime @default(now()) @map("created_at")`
- 更新时间：`updatedAt DateTime @updatedAt @map("updated_at")`

### 模型设计规范

- 表名：snake_case 复数，通过 `@@map("table_name")` 映射
- 字段名：TypeScript 侧 camelCase，数据库侧通过 `@map("snake_case")` 映射
- ID：使用 `cuid()` 生成（`@id @default(cuid())`）
- 关联：必须显式定义 `onDelete` 行为（`Cascade` / `SetNull` / `Restrict`）
- 索引：查询频繁的字段必须建立索引
- **禁止**将大文件直接存入数据库
- 缓存字段（如 `favoriteCount`）的更新必须与关联操作在同一 `$transaction` 中
- `address + port` 联合唯一约束防止重复服务器

### 当前数据模型概览

- **User**：用户（role、isBanned、emailVerified、bio）
- **Server**：服务器（ownerId、status 审核状态、isVerified 认领状态、缓存的在线状态字段）
- **ServerStatus**：服务器状态历史记录（ping 结果）
- **Comment**：评论（自关联实现 2 层嵌套回复）
- **Favorite**：收藏（userId + serverId 唯一约束）
- **Notification**：通知（type 区分不同类型）
- **Account / Session / VerificationToken**：NextAuth 相关

---

## 7. Worker 规则

### 任务类型

| 队列名称 | 功能 | 调度方式 |
| --- | --- | --- |
| `server-ping` | 定时 ping 所有服务器 | 每 5 分钟自动调度 |
| `server-verify` | MOTD Token 认领验证 | 用户手动触发 |

### 任务幂等性

- 队列任务 **必须是幂等的**（重复执行不产生副作用）
- 使用 `jobId` 做去重

### 失败处理

- ping 任务：不重试（`attempts: 1`），下一轮自动覆盖
- verify 任务：不重试，用户可手动重试
- 成功任务保留最近 100 条（`removeOnComplete: 100`）
- 失败任务保留最近 50 条（`removeOnFail: 50`）

### 并发控制

- ping Worker 并发数：5（`concurrency: 5`）

### 通知触发

- 服务器从离线变在线时，通知所有收藏者（Redis 1 小时冷却防刷）
- 通知创建失败不影响主流程

### Worker 进程管理

- Worker 作为独立进程运行（`pnpm worker`）
- 开发时两个终端：`pnpm dev` + `pnpm worker`
- 新增/修改 Worker 后需重启 Worker 进程

---

## 8. 认证规则

### NextAuth v5 配置

- 使用 Credentials Provider + JWT session 策略
- `PrismaAdapter` 为未来 OAuth 预留
- session 中包含 `user.id`、`user.name`、`user.image`
- 更新用户资料后通过 `useSession().update()` 刷新客户端 session
- jwt callback 处理 `trigger === "update"` 以同步最新数据

### 邮箱验证流程

- 注册时发送 6 位数字验证码（Nodemailer + 飞书 SMTP）
- 验证码存 Redis（TTL 10 分钟）
- 验证通过后创建用户，`emailVerified` 设为当前时间
- 未验证邮箱无法登录

### 找回密码

- 与注册验证码共用 Redis 基础设施，用不同 prefix 隔离
- 不论邮箱是否存在都返回相同响应（防枚举）

---

## 9. 文件存储规则

### 开发环境：本地存储

- 存储目录：`public/uploads/`（avatars / server-icons）
- 统一输出 WebP 格式
- 路径格式：`/uploads/avatars/{userId}.webp`、`/uploads/server-icons/{serverId}.webp`
- `public/uploads/` 已加入 `.gitignore`

### 生产环境：阿里云 OSS（上线时切换）

- 将 `src/lib/storage.ts` 替换为 OSS 实现
- 或通过 `STORAGE_TYPE` 环境变量切换

---

## 10. 代码风格

### TypeScript 严格模式

- `tsconfig.json` 中 `strict: true` **不可关闭**
- **禁止**使用 `any` 类型（ESLint 已配置 `@typescript-eslint/no-explicit-any: "error"`）
- 需要宽泛类型时使用 `unknown` + 类型守卫
- 类型导出使用 `export type`
- 类型导入使用 `import type`

### 导入顺序（从上到下）

1. Node.js 内置模块（`import { readFile } from "fs/promises"`）
2. 第三方库（`import { z } from "zod"`）
3. `@/` 路径别名导入（`import { db } from "@/lib/db"`）
4. 相对路径导入（`import { helper } from "./utils"`）
5. 类型导入放在同组最后（`import type { ... } from "..."`）

### 错误处理

- API Route **必须** 有 `try-catch` 包裹
- 错误响应统一格式：`{ error: string, details?: unknown }`
- 未登录：401 `{ error: "请先登录" }`
- 无权限：403 `{ error: "无权限" }`
- 资源不存在：404 `{ error: "未找到" }`
- 校验失败：400 `{ error: "校验失败", details: zodError.flatten() }`
- 重复资源：409 `{ error: "已存在" }`
- 通知/副作用操作失败只记日志，不阻塞主操作

### 导出规范

- 组件：命名导出（`export function ServerCard`）
- 工具函数：命名导出
- 类型：`export type` / `export interface`
- 常量：命名导出
- **禁止** default export（Next.js 页面组件除外）

---

## 11. 禁止事项（红线）

1. ❌ **不要**在 API Route 中直接 ping Minecraft 服务器
2. ❌ **不要**使用 `any` 类型
3. ❌ **不要**写未经 Zod 校验的直接入库操作
4. ❌ **不要**在 Server Component 中使用 `useState` / `useEffect`
5. ❌ **不要**在提交中包含 `.env` 文件
6. ❌ **不要**在生产环境使用 `db push`
7. ❌ **不要**把大文件直接存入数据库
8. ❌ **不要**用 `dangerouslySetInnerHTML` 渲染用户输入内容（JSON-LD 等可控内容除外）
9. ❌ **不要**硬编码配置值（端口、URL、超时等），统一使用环境变量或常量
10. ❌ **不要**在前端只靠隐藏按钮做权限控制，服务端必须校验
11. ❌ **不要**引入未经评估的重型依赖（先评估包大小和必要性）
12. ❌ **不要**让通知/副作用失败阻塞主操作流程
13. ❌ **不要**在上传区域显示技术性词汇（WebP、裁切、压缩）
14. ❌ **不要**逐个请求收藏状态，使用批量查询
